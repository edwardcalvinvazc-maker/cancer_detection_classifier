# cancer_detection_classifier
Cancer detection using the ensemble method applying the stacking classifier with base classifier as b1:RandomForestClassifier b2:HistGradientBoostingClassifier and final classifier as LogisticRegression
